# 测试指南

## 运行测试

### 后端测试

```bash
cd backend
pytest tests/ -v
```

### 前端测试

```bash
cd frontend
npm test
```

## 测试用例

### 1. 数据层测试

| 编号 | 用例 | 说明 | 预期结果 |
|------|------|------|---------|
| D-01 | 假数据完整性 | 验证生成的邮件数据字段完整 | 所有必填字段非空 |
| D-02 | 附件文件存在 | 验证引用的附件文件确实存在 | 每个附件路径可访问 |
| D-03 | 附件格式覆盖 | 验证覆盖所有要求的文件格式 | pdf/docx/xlsx/pptx/txt/csv/png/jpg/zip 均存在 |
| D-04 | 附件可解析 | 验证每个附件都能被正确解析为文本 | 解析结果非空 |

### 2. 文件解析测试

| 编号 | 用例 | 说明 | 预期结果 |
|------|------|------|---------|
| P-01 | PDF 解析 | 解析 PDF 文件提取文本 | 返回正确文本内容 |
| P-02 | DOCX 解析 | 解析 Word 文档 | 返回正确文本内容 |
| P-03 | XLSX 解析 | 解析 Excel 表格 | 返回表格数据文本 |
| P-04 | PPTX 解析 | 解析 PPT 演示文稿 | 返回幻灯片文本 |
| P-05 | CSV 解析 | 解析 CSV 文件 | 返回表格数据 |
| P-06 | TXT 解析 | 解析纯文本文件 | 返回原始文本 |

### 3. 索引与检索测试

| 编号 | 用例 | 说明 | 预期结果 |
|------|------|------|---------|
| R-01 | 邮件索引 | 对邮件建立向量索引 | 索引创建成功，数量匹配 |
| R-02 | 正文检索 | 按邮件正文内容检索 | 返回相关邮件，相似度排序 |
| R-03 | 附件内容检索 | 按附件内容检索 | 返回包含相关附件的邮件 |
| R-04 | 检索结果来源 | 验证检索结果包含来源信息 | source 字段完整 |

### 4. 对话管理测试

| 编号 | 用例 | 说明 | 预期结果 |
|------|------|------|---------|
| C-01 | 创建对话 | POST /api/conversations | 返回新对话 ID |
| C-02 | 对话列表 | GET /api/conversations | 返回所有对话 |
| C-03 | 对话详情 | GET /api/conversations/{id} | 返回对话含消息历史 |
| C-04 | 重命名对话 | PUT /api/conversations/{id} | 名称更新成功 |
| C-05 | 删除对话 | DELETE /api/conversations/{id} | 对话被删除 |
| C-06 | 发送消息 | POST /api/conversations/{id}/messages | 返回 AI 回答+来源 |
| C-07 | 多轮对话 | 连续发送多条消息 | 后续回答能引用上下文 |

### 5. Memory 测试

| 编号 | 用例 | 说明 | 预期结果 |
|------|------|------|---------|
| M-01 | Memory 自动生成 | 对话中产生 Memory 记录 | Memory 条目被创建 |
| M-02 | Memory 查询 | GET /api/memory | 返回所有 Memory |
| M-03 | Memory 删除 | DELETE /api/memory/{id} | Memory 被删除 |
| M-04 | Memory 影响检索 | Memory 信息影响后续检索结果 | 相关结果优先级提升 |

### 6. 前端测试

| 编号 | 用例 | 说明 | 预期结果 |
|------|------|------|---------|
| F-01 | 对话列表渲染 | 侧边栏显示对话列表 | 列表正确显示 |
| F-02 | 新建对话 | 点击新建按钮 | 创建并切换到新对话 |
| F-03 | 发送消息 | 输入文本并发送 | 消息显示在聊天区域 |
| F-04 | 来源卡片 | AI 回答包含来源 | 来源卡片正确渲染 |
| F-05 | 对话切换 | 点击侧边栏对话 | 切换到对应对话 |
| F-06 | 响应式布局 | 缩小窗口宽度 | 布局正确适配 |

### 7. 集成测试

| 编号 | 用例 | 说明 | 预期结果 |
|------|------|------|---------|
| E-01 | 端到端问答 | 前端提问 → 后端检索 → 返回答案 | 完整流程正常 |
| E-02 | 多轮问答 | 连续多轮提问 | 上下文正确维护 |
| E-03 | 跨对话 Memory | 在对话 A 建立 Memory，在对话 B 验证 | Memory 跨对话生效 |
